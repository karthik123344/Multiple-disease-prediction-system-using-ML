# Requirements for Multiple Disease Prediction System

## Install dependencies

To install the required packages, create a virtual environment and use the following command:

```
pip install -r requirements.txt
```

## Usage

Instructions on how to use the Multiple Disease Prediction System will be added here.